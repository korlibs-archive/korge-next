name: "Caches gradle & Kotlin/Native cleanup"
description: "Caches gradle & Kotlin/Native cleanup"
inputs: {}
runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        rm -rf ~/.gradle/caches/modules-2/modules-2.lock
        rm -rf ~/.gradle/caches/modules-2/gc.properties
        rm -rf ~/.konan/cache
